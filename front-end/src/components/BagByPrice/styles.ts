import styled from "styled-components";

export const Container = styled.div`
  .image {
    cursor: pointer;
    text-align: left;
    border-radius: 7px;
  }

  > span {
    font-size: 1.2rem;
    padding-left: 1rem;
  }
`;
